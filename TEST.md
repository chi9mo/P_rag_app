# Phase 5 統合テスト手順

## 事前確認

- Ollama が起動していること（メニューバーにアイコンがあること）
- プロジェクトルート（`P_rag_app/`）にいること

---

## アプリの起動

```bash
uv run python -m rag_app.app
```

ターミナルに以下が表示されることを確認する。

```
インデックスが既に存在します。スキップします。   ← 2回目以降の起動
Running on local URL: http://127.0.0.1:7860
```

ブラウザで `http://127.0.0.1:7860` を開く（自動で開かない場合は手動でアクセス）。
岩本「自動で開かなかった。手動で開いた。」

---

## テスト 1: 初回インデックス構築のスキップ確認

### 目的
`qdrant_storage/` が存在する場合、インデックス構築がスキップされること。

### 手順
1. アプリを起動する
2. ターミナルの出力を確認する

### 期待結果
```
インデックスが既に存在します。スキップします。
```
岩本「OK」

### 初回起動をテストしたい場合（オプション）
```bash
# qdrant_storage を削除してから起動すると初回状態を再現できる
rm -rf qdrant_storage/
uv run python -m rag_app.app
```
期待結果:
```
インデックスを構築中...
インデックス構築完了: 1722 チャンク
```
岩本「
OK
want：長いので、構築中のビジュアライズしたい。」

---

## テスト 2: ストリーミング回答

### 目的
質問を送信したとき、回答がリアルタイムで逐次表示されること。

### 手順
1. テキストボックスに以下を入力する
   ```
   この資料はどのような内容ですか？
   ```
2. 「送信」ボタンをクリックするか、Enter キーを押す

<!-- ! イマココ！ -->
### 確認ポイント
- [ ] 送信後すぐにテキストボックスが空欄になる
  - 要確認：すぐに空欄ならない。アシスタントの回答が完了すると、空欄になる。
- [ ] チャット欄に自分のメッセージが表示される
- [ ] アシスタントの回答がトークン単位で少しずつ表示される（一気に表示されない）
- [ ] 回答が完全に表示されるまで待てること
岩本「
want:メモリスワップが発生している。なるべくなくても済むように、メモリ効率を上げたい。
」

---

## テスト 3: 出典表示

### 目的
回答に使用されたチャンクのページ番号と引用テキストが表示されること。

### 手順
1. テスト 2 の質問・回答が完了した状態で実施する
2. チャット欄の下にある「出典」をクリックしてアコーディオンを開く

### 確認ポイント
- [ ] 「出典」アコーディオンが開閉できる
- [ ] `p.XX` 形式でページ番号が表示されている
- [ ] 各出典に引用テキストの冒頭が表示されている
- [ ] 複数の出典（最大 4 件）が表示されている

---

## テスト 4: 資料に記載のない質問

### 目的
資料に存在しない情報を聞いたとき、「資料に記載がありません」と返ること。

### 手順
1. テキストボックスに以下を入力して送信する
   ```
   今日の東京の天気を教えてください
   ```

### 確認ポイント
- [ ] 「資料に記載がありません」という旨の回答が返ること
- [ ] 架空の情報を作って回答しないこと

---

## テスト 5: 多ターン会話（会話履歴）

### 目的
複数回の質問にわたって文脈が保持されること。

### 手順
1. 1問目を送信する
   ```
   この資料の主な対象読者は誰ですか？
   ```
2. 回答を確認したあと、2問目を送信する
   ```
   その人たちはどのような場面でこの資料を使いますか？
   ```

### 確認ポイント
- [ ] 2問目の回答が1問目の文脈を踏まえている（「その人たち」が何を指すか理解している）
- [ ] チャット欄に両方の質問と回答が表示されている

---

## テスト 6: リセットボタン

### 目的
「リセット」ボタンで会話履歴がクリアされること。

### 手順
1. テスト 5 の後（複数回の会話がある状態）に実施する
2. 「リセット」ボタンをクリックする

### 確認ポイント
- [ ] チャット欄が空になる
- [ ] 「出典」アコーディオンの内容が空になる
- [ ] リセット後に新しい質問を送信しても正常に動作する

---

## テスト 7: Enter キー送信

### 目的
テキストボックスで Enter キーを押すと送信されること。

### 手順
1. テキストボックスに質問を入力する
2. マウスを使わず Enter キーだけで送信する

### 確認ポイント
- [ ] Enter キーで送信できる
- [ ] 送信後にテキストボックスが空欄になる

---

## テスト結果の記録

| テスト | 結果 | 備考 |
|---|---|---|
| 1: インデックススキップ | - | |
| 2: ストリーミング回答 | - | |
| 3: 出典表示 | - | |
| 4: 資料外の質問 | - | |
| 5: 多ターン会話 | - | |
| 6: リセットボタン | - | |
| 7: Enter キー送信 | - | |

---

## よくある問題と対処

| 症状 | 原因 | 対処 |
|---|---|---|
| アプリが起動しない | Ollama が停止している | メニューバーの ollama アイコンから起動 |
| 回答が返らず止まる | LLM モデルのロード中 | 初回は `qwen3:14b`（9GB）のロードに時間がかかる。待つ |
| `http://127.0.0.1:7860` に接続できない | ポート競合 | ターミナルに表示された URL を使う（7861 など別ポートになることがある） |
| 「資料に記載がありません」が返らない | システムプロンプトの挙動 | qwen3:14b は `<think>` タグで内部推論することがある。回答本文を確認する |
