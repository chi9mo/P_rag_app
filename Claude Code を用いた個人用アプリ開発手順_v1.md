# Claude Code を用いた個人用アプリ開発手順

> **対象**：Claude Code を使って自分用のアプリを開発したい人  
> **作成者**：diwamoto  
> **作成日**：2026-05-19

---

## 概要

Claude Code を使って個人用アプリを開発するための、仕様策定から本番運用までの標準手順。

開発は以下のドキュメントを軸に進める。Claude には常にこれらのファイルを参照させることで、長いセッションでも文脈がずれにくくなる。

| ファイル | 役割 |
|---|---|
| `CLAUDE.md` | Claude Code 向けのプロジェクト情報（技術スタック・制約・禁止事項など） |
| `SPEC.md` | アプリの仕様（何を作るか） |
| `SETUP.md` | 環境構築手順 |
| `PLAN.md` | 実装フェーズとタスクリスト |
| `TESTING.md` | テスト手順（フェーズごとに分割も可） |

---

## 手順

### 1. プロジェクトの初期化

プロジェクトディレクトリを作成し、Git リポジトリを初期化する。

```sh
git init
```

続けて `.gitignore` を作成する。環境依存のファイルや秘密情報がコミットされるのを防ぐため、**必ず最初に用意する**。

```
# プロンプト
.gitignore を作成して。
言語は [Python / Node.js / ...] を使う予定。
.env や OS のシステムファイルも除外すること。
```

```sh
git add .gitignore
git commit -m "init: add .gitignore"
```

---

### 2. 仕様の策定（SPEC.md / CLAUDE.md）

Claude Code を起動し、作りたいものを自由に相談する。  
ある程度イメージが固まってきたら、以下のプロンプトでファイルに書き出す。

```
# プロンプト
ここまでの仕様内容を SPEC.md にまとめて。
コンテキスト管理で必要なものは CLAUDE.md にまとめて。
```
#### CLAUDE.md に含めるべき主な情報（参考）

- 使用言語・フレームワーク・主要ライブラリ
- ディレクトリ構成の方針
- コーディング規約（命名規則、コメントの扱い等）
- やってはいけないこと（グローバル環境を汚さない、など）
- 外部サービスや API の利用状況

> [!NOTE]
> `SPEC.md` の加筆・修正は **影響範囲に十分注意**すること。  
> 仕様変更は実装済みのコードと矛盾が生じる可能性がある。

以降、`CLAUDE.md` および `SPEC.md` の内容は適宜更新していく。

```sh
git commit -m "docs: add SPEC.md and CLAUDE.md"
```

> [!TIP]
> これが最低限のコミット粒度。より細かくコミットしても構わない。

---

### 3. 環境構築（SETUP.md）

`SPEC.md` が概ね固まったら、環境構築手順を確認・作成する。

```
# プロンプト
これからこのプロジェクトの環境構築をします。
現状のインストール状況を確認した上で、環境構築の手順を教えて。

原則：グローバル環境は汚さない。ベストプラクティスに従う。

環境構築手順は SETUP.md にまとめて。
```

`SETUP.md` が固まったら、Claude と一緒に環境構築を実行する。

```sh
git commit -m "docs: add SETUP.md"
```

---

### 4. 実装プランの作成（PLAN.md）

以下のプロンプトで実装プランを作成する。

```
# プロンプト
CLAUDE.md、SETUP.md、SPEC.md を確認した上で、実装プランを作成して。
作成したプランは PLAN.md にまとめて。
各実装フェーズでタスクリストを作成すること。
```

```sh
git commit -m "docs: add PLAN.md"
```

---

### 5. フェーズごとの実装

フェーズ単位で実装を進める。

```
# プロンプト
フェーズ XX を実行して。
```

実装が完了したら、タスク単位でコミットする。  
最低でも **フェーズ完了ごとに 1 コミット**を目安にする。

```sh
git commit -m "feat: implement phase XX - [フェーズの概要]"
```

---

### 6. テストの実施（TESTING.md）

各フェーズ完了後、テストを行う。

```
# プロンプト
完了したフェーズ XX のテストを実施する。
テスト手順を作成し、TESTING.md にまとめて。
```

テスト手順を確認後、実際にテストを実行する。

#### テストが失敗した場合

```
# プロンプト
フェーズ XX のテストで [エラー内容] が発生した。
SPEC.md と PLAN.md を確認した上で、原因を特定して修正して。
```

修正後、再度テストを実施する。問題が解消したらコミットする。

```sh
git commit -m "test: phase XX passed / fix: [修正内容]"
```

> [!TIP]
> `TESTING.md` の量が増えてコンテキストが肥大化しそうであれば、  
> `TESTING_phase1-3.md` のようにファイルを分割する。

---

### 7. 実装完了まで繰り返す

**ステップ 5〜6 を、全フェーズの実装が完了するまで繰り返す。**

```
実装 → テスト → コミット → 次のフェーズへ
```

---

### 8. 最終テストと本番運用

全フェーズの実装が完了したら、最終テストを実施する。

```
# プロンプト
全フェーズの実装が完了した。
最終テストの手順書を作成して。
```

手順書を確認後、テストを実行し、問題がなければ本番運用を開始する。

---

## 詰まったときのプロンプト集

### Claude の提案がずれてきたと感じたとき

```
# プロンプト
SPEC.md と CLAUDE.md を再確認して、現在の実装との差分を洗い出して。
```

### エラーが解消しないとき

```
# プロンプト
以下のエラーが解消しない。
原因の仮説を3つ挙げた上で、それぞれの確認方法を示して。

[エラーメッセージ]
```

### コンテキストが長くなりすぎたとき

セッションが長くなると Claude の応答がずれやすくなる。  
その場合は `/clear` で会話をリセットし、新しいセッションで以下のプロンプトから再開する。

```
# プロンプト
CLAUDE.md、SPEC.md、PLAN.md を確認して。
現在はフェーズ XX まで完了しており、次はフェーズ XX に取り組む。
```

---

## コスト管理の注意点

- セッションが長くなるほどトークン消費が増える。こまめに `/clear` でリセットするのが有効。
- CLAUDE.md にプロジェクト情報を集約しておくと、新しいセッションでも素早く文脈を復元できる。
- 実装フェーズは細かく区切るほど、1 セッションあたりの負荷が下がる。
